---
name: Bug report
about: Something broke — tools, follow, attachments, config, or credential handling
labels: bug
---

**What happened**

**Expected behavior**

**Environment**
- OMP version:
- omp-discord version (`omp plugin list`):
- Install method (npm / `omp plugin link`):
- OS / secret service backend:

**Operation shape (never content)**
- Tool or command (e.g. `discord_follow_start`, `/discord set`):
- Target kind (guild / dm / group-dm):
- Follow state (`/discord follow status` output — no message bodies):

**Logs**
Relevant OMP log excerpts (`OMP_DISCORD_DEBUG=1` ledger lines are metadata-only). Strip any message content, attachment content, or credentials before posting.
